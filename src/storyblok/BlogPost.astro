---
import { storyblokEditable, renderRichText } from "@storyblok/astro";
import MailingForm from "../components/MailingForm.svelte";

const { blok } = Astro.props;
const content = renderRichText(blok.content);
---

<article {...storyblokEditable(blok)}>
  <h1 class="text-3xl text-center pt-10 pb-5 text-teal-900">
    {blok.title}
  </h1>
  <hr class="mb-5 w-1/2 m-auto" />
  <img
    src={blok.startImage}
    class="lg:w-1/2 p-10 lg:px-0 mx-auto mb-16"
    alt=""
  />
  <div
    class="text-lg text-justify mx-10 md:mx-auto prose prose-p:mb-8 prose-h2:mb-4 prose-h2:text-teal-900 prose-img:max-h-96 prose-img:mx-auto prose-img:mb-0 prose-h5:text-sm prose-h5:text-center"
    set:html={content}
  >
  </div>
</article>
<div class="flex justify-center my-5">
  <MailingForm client:visible />
</div>
